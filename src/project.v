/*
 * This template file has been replaced by:
 *   - fmac8.v             (FMAC datapath)
 *   - tt_um_Logy_FMAC.v   (Tiny Tapeout top module)
 *
 * It is intentionally left empty (no module) and is NOT listed in
 * info.yaml `source_files` or test/Makefile `PROJECT_SOURCES`.
 * Safe to delete.
 */
